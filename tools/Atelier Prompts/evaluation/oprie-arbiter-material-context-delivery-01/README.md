# OPRIE-ARBITER-MATERIAL-CONTEXT-DELIVERY-01 — preuves

| Fichier | Contenu |
| --- | --- |
| `runs.jsonl` | 30 tours, joints aux traces Worker (contexte livré à l'Arbitre, provenance, champs) |
| `controls.jsonl` | contrôle négatif `deep_content_available = false`, 6 tours |
| `summary.json` | chemin réel, gap à deux étages, correctif, avant/après, contrôle négatif |

Comparatifs : `evaluation/oprie-input-availability-field-01/runs.jsonl` (16 blocked) et
`evaluation/oprie-critic-material-context-delivery-01/runs.jsonl` (10 blocked).
Matériau synthétique uniquement ; aucun contenu brut ne quitte l'Analyste.
