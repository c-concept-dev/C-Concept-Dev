// Fixture synthétique MONO-00 — module d'exemple pour tester la mécanique
// de vérification de manifeste, sans dépendre d'un lot gelé réel.
function add(a, b) { return a + b; }
module.exports = { add };
