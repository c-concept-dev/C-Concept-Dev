#!/usr/bin/env node
"use strict";
/** MONOLITH v1.0 — tools/build-manifest.js : MANIFEST.json + SHA256SUMS.txt du paquet (hors runs/, hors les deux fichiers produits). */
const fs = require("fs"), path = require("path"), crypto = require("crypto");
const ROOT = path.resolve(__dirname, "..");
const sha = (p) => crypto.createHash("sha256").update(fs.readFileSync(p)).digest("hex");
function walk(dir, out) { fs.readdirSync(dir).sort().forEach(function (f) { const p = path.join(dir, f); const st = fs.statSync(p); if (st.isDirectory()) { if (["runs", "node_modules", "scratch"].indexOf(f) === -1) walk(p, out); } else out.push(p); }); return out; }
function build() {
  const files = walk(ROOT, []).map((p) => path.relative(ROOT, p)).filter((r) => r !== "MANIFEST.json" && r !== "SHA256SUMS.txt" && !r.endsWith(".zip"));
  const entries = files.map((r) => ({ path: r, sha256: sha(path.join(ROOT, r)), bytes: fs.statSync(path.join(ROOT, r)).size }));
  const cfg = JSON.parse(fs.readFileSync(path.join(ROOT, "config", "monolith.config.json"), "utf8"));
  /* v1.0.4 — PROVENANCE MESUREE (jamais tapee) : sceau MONO-11 par la garde reelle, sceaux des lots geles verifies, resultats de tests lus, contrat de validation lu dans lib/llm.js */
  const P = require("../lib/paths.js"); const SP = require("../lib/stage-professionals.js"); const { SEAL } = SP.loadSealedMono11(); const lots = P.verifyFrozenLots();
  const frozenMeasured = {}; Object.keys(lots).forEach((k) => { frozenMeasured[k] = { files: lots[k].files, divergences: lots[k].divergences.length, sealSha256: lots[k].sealSha256 || null, canonicalZipSha256: lots[k].canonicalZipSha256 || null }; });
  const resultsPath = path.join(ROOT, "test", "results.json"); const tr = fs.existsSync(resultsPath) ? JSON.parse(fs.readFileSync(resultsPath, "utf8")) : null;
  /* v1.0.12 : le contrat de validation n'est plus epingle dans lib/llm.js ; il est MESURE en chargeant le module (source de verite = config.frozenLots[MONO-11].contractVersion) */
  const llmSrc = fs.readFileSync(path.join(ROOT, "lib", "llm.js"), "utf8");
  const vc = /VALIDATION_CONTRACT = "([^"]+)"/.exec(llmSrc) || (/VALIDATION_CONTRACT = \(P\.CONFIG\.frozenLots/.test(llmSrc) ? [null, cfg.frozenLots["MONO-11"].contractVersion] : null);
  const integration = { mono11: { version: SEAL.mono11Version, zipSha256: SEAL.mono11ZipSha256, runtimeSealSha256: SEAL.runtimeSealSha256, sourceSealHash: SEAL.runtimeSealSha256, runCodeHash: SEAL.runCodeHash, manifestSha256: SEAL.mono11ManifestSha256 || null, validationContract: vc ? vc[1] : null,
      note: "sourceSealHash = runtimeSealSha256 du lot MONO-11 charge : cle de contexte de la reuse (lib/llm.js reuseContextCheck) ; validationContract = contrat SCIENTIFIQUE (inchange), distinct de la version du code" },
    predecessor: { version: "MONOLITH-v1.0.12", zipSha256: null, status: "GELE FROZEN_WITH_RESERVATIONS (ACTIVATION_BLOCKED_BY_RA2) — base de reprise ; la candidate v1.0.13 est REJETEE apres audit independant (V1_0_13_FREEZE_AUDIT_FAIL : autorite BRUTE au lieu du document NORMALISE)", mono11: "v0.4 (zip 79c16d08..., contrat MONO-11-v3), inchange" },
    /* v1.0.5 — autorite de tarification mesuree (version + hash du snapshot canonique) */
    pricing: (function () { try { const pr = require("../lib/pricing.js").createPricing(); return { version: pr.version, snapshotHash: pr.hash, models: pr.snapshot().models.map((m) => m.model) }; } catch (e) { return { error: e.code || e.message }; } })(),
    frozenLotsMeasured: frozenMeasured, tests: tr ? { total: tr.total, passed: tr.passed, failed: tr.failed, ranAt: tr.ranAt } : null,
    professionals: { maxCandidatesToEvaluate: cfg.professionals.maxCandidatesToEvaluate, selection: "PROFESSIONAL-SELECTION-v1 (inchangee)", capIsGuardNotTarget: true, earlyStopPolicy: "NONE" },
    transport: { streaming: cfg.llm.streaming ? { enabled: cfg.llm.streaming.enabled, inactivityMs: cfg.llm.streaming.inactivityMs, maxTotalMs: cfg.llm.streaming.maxTotalMs } : null, proxy: "worker/evidenceforge-llm-proxy-v0.8 (= v0.7 relais SSE + HEADER_TIMEOUT_MS separe, defaut 60000 ms bornes 1000-90000, lecture stricte des vars, gardes de flux = client ; MONO-08 v0.6 gele et v0.7 (MONOLITH-v1.0.8) inchanges)", observability: cfg.observability || null },
    ef03b: { adapter: "lib/ef03b-resilience.js (additif autour de llmCall ; MONO-11 gele inchange)", singleNormalizedDocumentAuthority: "v1.0.14 (R-A2) : documentAuthority(targetId) OBLIGATOIRE et VERIFIEE — doit rendre le contenu NORMALISE examine par MONO-11 (core/target-normalizer.js, module GELE) ; point fixe des regles verifie a chaque passe ; autorite absente / vide / non normalisee => fail closed fatal ; jamais de repli sur le brut ni sur le prompt ; registre lie a documentAuthoritySha256", registryImportAuthority: "v1.0.14 (B4) : tools/ef03b-registry-import.js calcule l empreinte sur le document NORMALISE du run ; autorite manquante ou non normalisee => IMPORT FAIL CLOSED", traceParity: "v1.0.14 (R-A1) : reparation illisible => aucune recomposition miroir, trace REPAIR_UNPARSABLE, dimensions fautives du lot", mirrorAlignedWithLot: "v1.0.12 (R5) : la branche isTargeted applique RE.mergeRepairLineage du lot gele MONO-11 v0.4 avant RE.recompose — le candidat trace / inscrit au registre est byte-identique a celui que le lot valide", contractFromConfig: "v1.0.12 (R6) : lib/llm.js, l adaptateur et tools/ef03b-registry-import.js lisent contractVersion dans config.frozenLots[MONO-11] (MONO-11-v3) ; import hors ligne fail-closed (sceau et contrat)", lineagePreservation: "v1.0.11 : recomposition apres litteralisation = dedup(VALID_ORIGINAL_REFS ++ VALID_REPAIRED_REFS) (mergeLiteralRefs / preserveLineageRepairs, fonctions pures ; controle de litteralite du lot gele reproduit) — une reference originale deja litterale n est jamais supprimee ; FINAL ⊇ VALID_ORIGINAL, FINAL ⊆ RAW ∪ REPAIR", reviewMaxTokens: cfg.llm.reviewMaxTokens, reviewResilience: (function () { const o = Object.assign({}, cfg.llm.reviewResilience || {}); delete o.$comment; return o; })(), registry: "reviews-valid.jsonl (par run ; cle = sha256 du prompt EF-03B de passe 1 + sceau + contrat)", replay: "POST /api/runs/:id/replay-reviews (depuis le checkpoint professionnel ; aval seul)" },
    provenance: "MONOLITH v1.0.14 = v1.0.12 GELEE + SINGLE NORMALIZED DOCUMENT AUTHORITY (R-A2 : la litteralite est jugee sur le document NORMALISE que MONO-11 examine, jamais sur le brut ni sur une reconstruction du prompt) + RA1 TRACE PARITY + correctif d import de registre (B4) — la candidate v1.0.13 (autorite BRUTE) est REJETEE et conservee comme artefact historique. MONOLITH v1.0.12 = MONOLITH v1.0.12 = v1.0.11 (inchangee : correctif de litteralisation, transport streame, proxy v0.8, Panel Sufficiency, economic-panel, stage-professionals) + LOT GELE MONO-11 v0.4 (TARGETED REPAIR LINEAGE PRESERVATION, contrat MONO-11-v3 : LINEAGE_MONOTONICITY_DURING_REPAIR) : GLOBAL LINEAGE PRESERVATION sur les deux chemins connus — litteralisation (adaptateur, v1.0.11) et reparation ciblee (lot gele, v0.4). Conditions d integration de l audit independant v1.0.11 traitees : R5 (miroir de l adaptateur aligne sur le lot : RE.mergeRepairLineage avant RE.recompose) et R6 (contrat de validation lu dans la configuration du lot ; registre et cache fail-closed inter-sceau / inter-contrat ; import hors ligne refusant un autre sceau ou un autre contrat). MONOLITH v1.0.11 = v1.0.10 (inchangee : prompts, validateurs, registre, troncature, agregation, rapport, transport, proxy v0.8, lots geles) + EF-03B LITERALIZATION LINEAGE PRESERVATION : lib/ef03b-resilience.js bloc G — la recomposition apres litteralisation conserve les references originales deja litterales et ajoute les references reparees litterales (fusion pure, ordre brut puis modele, deduplication stable) au lieu de remplacer le tableau entier ; cause racine v1.0.10 = references litterales exclues des propositions puis RE.recompose (gele) remplacant le tableau : 12 citations valides perdues et 3 constats vides au run efm-20260918-a64167c0 t.10 (audit independant + reconstruction hors ligne LINEAGE_RESTORATION_PASS) ; tests T-EF03B-27..35 (T1..T6, non-regression semantique, purete, shadow historique lecture seule). MONOLITH v1.0.10 = v1.0.9 (inchangee : transport streame, proxy v0.8, Panel Sufficiency, economic-panel, stage-professionals) + EF-03B REVIEW RESILIENCE : adaptateur additif lib/ef03b-resilience.js autour de llmCall (registre des revues logiques VALID servies a 0 appel puis revalidees par le lot gele ; contrat de budget de sortie = forme seule ; max_tokens dimensionne au contrat (12288) ; troncature => REVIEW_OUTPUT_TRUNCATED, sauvetage structurel des findings complets + completion ciblee des dimensions manquantes, fusion deterministe ; litteralisation sur extraits (spans exacts proposes, choix du modele, empreinte de contexte) ; replay cible des revues depuis le checkpoint professionnel ; trace EF-03B par passe ; outils d autopsie / import de registre hors ligne) — correctif des 8 revues ERROR (21 sorties tronquees a 8192, 173 citations non litterales dont 74 % par mise en forme Markdown) du run reel (EF03B-REAL-RUN-AUTOPSY-v1.md). v1.0.9 = v1.0.8 (inchangee cote client : streaming valide en reel) + proxy v0.8 : delais separes HEADER_TIMEOUT_MS (seul applique par le Worker, attente des en-tetes amont, defaut 60 s justifie par la mesure : max 7,0 s sur 56 appels reels, fenetre edge ~100 s, client premier octet 180 s) / STREAM_INACTIVITY_TIMEOUT_MS / STREAM_MAX_DURATION_MS (gardes client, annoncees jamais appliquees), lecture stricte des variables Cloudflare (chaine/nombre, bornes, defaut sur + trace non sensible), alias TIMEOUT_MS herite — correctif du 504 upstream_timeout a 8,187 s (ROOT-CAUSE-504-8S-AUTOPSY-v1.md). v1.0.8 = v1.0.7 (inchangee : Panel Sufficiency, economic-panel, contrats scientifiques, semantique et independance des jumeaux, fanout 1 revue par jumeau x cible dossier) + STREAMING TRANSPORT (Anthropic stream:true -> Worker v0.7 relais SSE -> client lib/llm-stream.js reconstruction canonique deterministe -> validation / cache / reuse / ledger inchanges ; interruption fail-closed PROVIDER_STREAM_INTERRUPTED, aucun retry automatique, cout connu inscrit) + LIVE OBSERVABILITY (lib/live-status.js : live-status.json, state.live, heartbeat borne, trace console optionnelle EVIDENCEFORGE_TRACE_REVIEWS=1) — correctif des HTTP 524 edge sur les revues de dossier (ROOT-CAUSE-524-AUTOPSY-v1.md). v1.0.7 : ECONOMIC PANEL OPTIMIZATION + EARLY SUFFICIENCY + MARGINAL VALUE CONTROL + REVIEW FANOUT CONTROL : politique SUFFICIENCY_FIRST (lib/economic-panel.js) — panel initial compact, lots d expansion reevalues, contribution marginale par composantes, DIMINISHING_INFORMATION_RETURNS descriptif, arret budgetaire AVANT un lot (PANEL_INCOMPLETE_BUDGET_LIMIT reprenable, jamais une suffisance), plafond = garde ; cibles de revue = dossier de mission (1 revue complete par jumeau et par mission, PER_DOCUMENT explicite) ; compteurs reviewDocuments / reviewItems distincts ; projection avant la premiere revue, ECONOMIC_OUTLIER_WARNING, porte humaine ECONOMIC_REVIEW ; PANEL-SUFFICIENCY-v2 inchangee = seule autorite scientifique ; lots geles inchanges",
    status: "CANDIDAT (v1.0.14) — NON GELE : correctif B1/B2/B3/B4 ; 313 tests passes (292 de v1.0.12 conserves + RA2-01..08, RA1-01..03, RA2-E2E-01..08, RI-01..07) ; matrice de mutation M1..M6 : chaque mutant tue par un test nomme ; audit independant requis AVANT gel ; ACTIVE_VERSION inchangee (MONOLITH-v1.0.10) ; aucun run reel ; smoke reel R12 requis avant activation ; statut de lignee maximal avant audit : GLOBAL_LINEAGE_PRESERVATION_CANDIDATE" };
  const manifest = { schema: "EvidenceForge.MonolithManifest", product: cfg.product, generatedAt: new Date().toISOString(), frozenLots: cfg.frozenLots, integration: integration, files: entries, fileCount: entries.length,
    contentHash: crypto.createHash("sha256").update(entries.map((e) => e.sha256 + "  " + e.path).join("\n")).digest("hex") };
  fs.writeFileSync(path.join(ROOT, "SHA256SUMS.txt"), entries.map((e) => e.sha256 + "  " + e.path).join("\n") + "\n");
  fs.writeFileSync(path.join(ROOT, "MANIFEST.json"), JSON.stringify(manifest, null, 2) + "\n");
  return manifest;
}
function verify() { const lines = fs.readFileSync(path.join(ROOT, "SHA256SUMS.txt"), "utf8").trim().split("\n"); const bad = []; lines.forEach((l) => { const [h, r] = l.split(/\s+/); if (!fs.existsSync(path.join(ROOT, r)) || sha(path.join(ROOT, r)) !== h) bad.push(r); }); return { ok: bad.length === 0, files: lines.length, bad }; }
if (require.main === module) { if (process.argv[2] === "--verify") { const v = verify(); console.log(JSON.stringify(v)); process.exit(v.ok ? 0 : 1); } const m = build(); console.log("MANIFEST " + m.fileCount + " fichiers, contentHash " + m.contentHash); }
module.exports = { build, verify };
