import assert from "node:assert/strict";import vm from "node:vm";import {readFileSync} from "node:fs";const source=readFileSync(new URL("../src/core/fallback-core.js",import.meta.url),"utf8");const c={globalThis:null};c.globalThis=c;vm.runInNewContext(source,c);const x=c.JMMJSFallbackCore;const loop=[[1,43],[1.004,43],[1.008,43],[1.008,43.004],[1.004,43.004],[1,43.004],[1,43]];assert.equal(x.safeAnalyzeFallbacks({coords:loop,walkingMinutes:40,shortcutsRequested:true}).fallbacks.length,3);assert.equal(x.safeAnalyzeFallbacks({coords:[],walkingMinutes:30,shortcutsRequested:true}).status,"unknown");console.log("fallback-core: 2 vérifications réussies");